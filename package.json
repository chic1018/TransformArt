{
  "name": "transformart",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "packager": "electron-packager ./ TransformArt --win --out ./app --app-version=1.0.0 --electron-version=21.3.1 --overwrite --ignore=config.json --asar"
  },
  "author": "chic",
  "license": "ISC",
  "devDependencies": {
    "electron": "^21.3.1",
    "electron-packager": "^17.1.1"
  }
}